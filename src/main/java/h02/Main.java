package h02;

import java.util.*;

/**
 * Main entry point in executing the program.
 */
public class Main {
    private static final Random r = new Random(20L);

    /**
     * Main entry point in executing the program.
     *
     * @param args program arguments, currently ignored
     */
    public static void main(String[] args) {
    }
}
